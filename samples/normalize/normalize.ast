# Using for loop
fun norm(a):
    var result = 0
    for e in a:
        result += e²
    √(result)

# Using map function
fun norm(a) = √sum(a.map |x| => x²)

# Using vectorization
fun norm(a) = √sum(a.²)
