# Removes adjacent duplicates
fun clean1(input):
    zip(input, input[1..]).filter(|(x, y)| => x != y).map(|(x, y)| => y)

fun clean2(input) = input.filter(|x, y| => x != y).map(|(x, y)| => y)

fun clean3(input) = input.filter(|x, y| => x != y).$[1]
