# Using for loop
fun norm1(a):
    var result = 0
    for e in a:
        result += e²
    √(result)

# Using map function
fun norm2(a) = √sum(a.map |x| => x²)

# Using vectorization
fun norm3(a) = √sum(a.²)
