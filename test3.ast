import math [pi, sin, cos]
import game except [physics]

type Teacher <: Employee, Person:
    var subjects  
# this is a comment
    var class

#= 
	chfjfjfjfplmkglkhlk
	#= badood 
		#=
		=# bada bada doom!
	=#
=#

ns_"literal"
ns_'literal $name dndjfjfk hfhfhfjf'
ns_[this is sparta]

let johnName = Teacher(...args).name

let isHappy = true

fun add(a, b):
	a + b

if x == 5 and y == 6:
	print "Hello World!"

type Person(!name.`, !years.age`)

var name = "John [play song] Smith"
let pi = 3.14f
var salary = 1_200_999.56_NGN
let setList = set_[1, 2, 3]

let prototype = { model: "Camaro", make: "Chevrolet" }

fun factorial(n):
    | 1 -> 0
    | _ ->
        n * factorial(n - 1)
    
#----------------------------
# 05/07/17 
fun mean(list): 
    list.|sum() / size|

fun std(list): 
    let squaredDiff = sq(list - mean(list))
    let variance = squaredDiff.mean()
    sqrt(variance)

let list = [2, 45, 18, 6, 37, 9]
std(list)

var numPattern = /\d+/